# Dependencies of the vqtl module.
#
# vqtl reads genotype dosage from the genome-wide matrix already produced
# by gene_environment (RAW_FILE, see core/data.py) rather than parsing VCF
# files itself, so it needs no VCF-parsing or per-chromosome dask/pysnptools
# libraries of its own.
#
# The rest of gene_environment already requires pandas/numpy/scipy/
# scikit-learn/joblib/matplotlib/pyarrow/python-dotenv: they are listed
# here too for completeness, plus the libraries that are specific to vqtl
# (statsmodels for QuantReg/OLS/Logit, seaborn for the report's box/scatter
# plots, tabulate for the report's markdown tables, python-docx for the
# Word export).
numpy>=1.26
pandas>=2.0
pyarrow>=14.0
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.3
matplotlib>=3.8
seaborn>=0.13
joblib>=1.3
tabulate>=0.9
python-docx>=1.1
python-dotenv>=1.0
mysql-connector-python>=8.0
